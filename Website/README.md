# AI Terminal Website

Static documentation/landing site for AI Terminal v1.0.0.

Open `index.html` directly or host the folder with GitHub Pages, Cloudflare Pages, Netlify, etc.

The site uses plain HTML/CSS with a tiny JS file and no build step.
